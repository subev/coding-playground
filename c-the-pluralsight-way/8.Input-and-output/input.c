#include <stdio.h>

int main(int argc, const char *argv[])
{
    int pointa, pointb, pointc;
    scanf("%d %d %d", &pointa, &pointb, &pointb);

    printf("%d%d%d\n", pointa, pointb, pointb);

    char* someString;
    printf("Now let's add some string");
    scanf("%s", someString);
    printf("What you entered is:  %s", someString);

    return 0;
}
